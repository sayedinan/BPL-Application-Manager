# `styles/`

Global CSS. Tailwind directives live in `index.css`; design tokens and
component-level styles land here. CSP rules are enforced server-side
(see SPEC §8.5) — nothing in this folder should rely on `unsafe-inline`
for scripts.
